export interface IForm {
  number: string,
}
export interface IFormDate {
  "day": string,
  "month": string;
}
export interface IFact {
  "fact"?: string,
  "error"?: string;
}

// export { IForm, IFormDate, IFact };